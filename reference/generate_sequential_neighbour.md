# Generate neighbour for sequential or hierarchical designs

Generate neighbour for sequential or hierarchical designs

## Usage

``` r
generate_sequential_neighbour(
  design,
  swap,
  swap_within,
  level,
  swap_count,
  swap_all_blocks
)
```
