# Print one level's structure block (treatments, replication, spatial factors)

Print one level's structure block (treatments, replication, spatial
factors)

## Usage

``` r
.print_level_structure(x, lv, lab, indent, fmt_num)
```
