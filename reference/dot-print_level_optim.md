# Print one level's optimisation block (objective, score components, schedule)

Print one level's optimisation block (objective, score components,
schedule)

## Usage

``` r
.print_level_optim(x, lv, lab, indent, fmt_int, fmt_num)
```
