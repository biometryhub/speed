# Speed function for simple (non-hierarchical) designs

Speed function for simple (non-hierarchical) designs

## Usage

``` r
speed_simple(
  data,
  swap,
  swap_within,
  spatial_factors,
  iterations,
  early_stop_iterations,
  obj_function,
  quiet,
  seed,
  ...
)
```
