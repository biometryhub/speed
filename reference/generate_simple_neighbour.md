# Generate neighbour for simple (non-hierarchical) designs

Generate neighbour for simple (non-hierarchical) designs

## Usage

``` r
generate_simple_neighbour(
  design,
  swap,
  swap_within,
  level,
  swap_count,
  swap_all_blocks
)
```
