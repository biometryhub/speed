% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{.block_spread}
\alias{.block_spread}
\title{Replicate spread across blocks}
\usage{
.block_spread(df, swap, block)
}
\arguments{
\item{block}{Block factor column name.}
}
\description{
How many distinct blocks each treatment's replicates are spread over, and how
many treatments land more than once in a single block. This is the block
equivalent of \code{.replicate_spans()}: a block factor is nominal, so there is no
distance to measure along, but "how many different blocks do a treatment's
replicates reach" is still well defined. It reads the same treatment-by-block
incidence \code{M} as \code{\link[=.design_concurrence]{.design_concurrence()}} - the per-treatment figures come
from \code{M}'s rows, where concurrence uses the off-diagonals of \verb{M M'}.
}
\details{
Computed whenever a block factor is present, independent of the \code{concurrence}
argument: unlike concurrence it remains informative for complete blocks,
where it confirms every treatment reaches every block.
}
\keyword{internal}
