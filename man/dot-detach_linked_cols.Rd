% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.detach_linked_cols}
\alias{.detach_linked_cols}
\title{Set Linked Columns Aside Before The Search}
\usage{
.detach_linked_cols(data, optimise)
}
\arguments{
\item{data}{The design data frame, as the user passed it.}

\item{optimise}{Per-level \code{optimise} list as built by \code{\link[=create_speed_input]{create_speed_input()}}.}
}
\value{
A list holding the stripped \code{data}, \code{optimise} with each level's
\code{origin_col} set, the held \code{values}, the linked column \code{map}, the
\code{origin_cols} names and the input \code{col_order}.
}
\description{
Linked columns take no part in scoring, so they are lifted out of \code{data} and
held until the design is returned - leaving them out avoids copying them on
every iteration. Each swap column carrying linked columns is given a
provenance index (see \code{\link[=.origin_col_names]{.origin_col_names()}}) that follows its treatment
through the swaps, which is how the held values are put back in the
optimised order.
}
\keyword{internal}
