% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{.design_block_factor}
\alias{.design_block_factor}
\title{Detect a block-type factor for one level of a design}
\usage{
.design_block_factor(df, spatial_cols, rc, cc)
}
\arguments{
\item{spatial_cols}{Character vector of this level's spatial factor columns.}
}
\description{
Among \emph{that level's} spatial factors that are not the row or column factor,
prefer one named like \code{block}; otherwise take the first such factor.
Failing that, fall back to a column literally named \code{block}. The chosen factor
is surfaced in the concurrence output (\verb{[block: ...]}), so the choice is
visible to the user.
}
\details{
Resolved per level (rather than once from the union of every level's spatial
factors) so that a hierarchical design whose levels are blocked by different
factors doesn't have one level's block column applied to another.
}
\keyword{internal}
