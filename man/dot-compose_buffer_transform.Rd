% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buffers.R
\name{.compose_buffer_transform}
\alias{.compose_buffer_transform}
\title{Compose two buffer coordinate displacements}
\usage{
.compose_buffer_transform(old, new)
}
\arguments{
\item{old, new}{Transform records, each a list of \code{row} and \code{col} named
\code{c(scale, shift)} vectors. \code{old} may be \code{NULL}, for a first call.}
}
\value{
A transform record of the same shape.
}
\description{
Each \code{\link[=add_buffers]{add_buffers()}} call displaces the coordinates it is given, so stacking
calls composes the displacements: applying \code{new} on top of \code{old} gives
\code{scale = new$scale * old$scale} and \code{shift = new$scale * old$shift + new$shift}. Composing (rather than overwriting) is what lets
\code{.drop_buffer_rows()} recover the original coordinates from a design that has
been buffered more than once, e.g. \code{add_buffers(add_buffers(d, "row"), "col")}.
}
\keyword{internal}
