% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_design}
\alias{plot_design}
\title{Plot Experimental Design}
\usage{
plot_design(
  design_result,
  treatment_var = NULL,
  block_var = NULL,
  title = "Experimental Design",
  row_var = "row",
  col_var = "col"
)
}
\arguments{
\item{design_result}{Either the result from speed() function or a design matrix}

\item{treatment_var}{The name of the treatment variable (required if design_result is a dataframe)}

\item{block_var}{Optional name of the blocking variable to highlight with boundaries}

\item{title}{Plot title (default: "Experimental Design")}

\item{row_var}{Name of the row coordinate variable (default: "row")}

\item{col_var}{Name of the column coordinate variable (default: "col")}
}
\value{
A ggplot object displaying the experimental design
}
\description{
Creates a visual representation of an experimental design layout, with options to highlight blocks
and customize the appearance.
}
\examples{
# Create a simple design
df <- data.frame(
  row = rep(1:4, each = 3),
  col = rep(1:3, times = 4),
  treatment = rep(LETTERS[1:4], 3)
)

# Optimize the design
result <- speed(df, "treatment", seed = 42)

# Plot the optimized design
plot_design(result)

# Plot with custom treatment variable name
plot_design(result$design_df, treatment_var = "treatment")

}
