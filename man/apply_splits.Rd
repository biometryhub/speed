% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_utils.R
\name{apply_splits}
\alias{apply_splits}
\title{Apply Nested Splits to a Design Data Frame}
\usage{
apply_splits(df, splits, nrows, ncols, block_nrows, block_ncols)
}
\arguments{
\item{df}{A data frame with \code{row} and \code{col} columns (and \code{block} if blocking
is in use), as built by \code{\link[=initialise_design_df]{initialise_design_df()}}.}

\item{splits}{Deprecated; use \code{\link[=initialise_split_design_df]{initialise_split_design_df()}} instead. A named
list of nested-unit specifications, ordered from the
outermost level to the innermost. Each entry is itself a list with
\code{nrows} and \code{ncols} (the dimensions of one unit at that level, in cells)
and an optional \code{items} (treatments to allocate across the units at that
level, one item per unit, ordered by parent then within-parent ID).
For each level, \verb{<name>} and \verb{<name>_treatment} columns are added (the
latter only if \code{items} is provided). Used to build hierarchical layouts
such as split-plot, split-split-plot, and strip-plot designs.
(default: \code{NULL})}

\item{nrows}{Number of rows in the design (default: \code{NULL})}

\item{ncols}{Number of columns in the design (default: \code{NULL})}

\item{block_nrows}{Number of rows in each block (default: \code{NULL})}

\item{block_ncols}{Number of columns in each block (default: \code{NULL})}
}
\value{
The data frame with one ID column per split (named after the split)
and one \verb{<name>_treatment} column per split that supplies \code{items}.
}
\description{
Adds nested-unit ID columns (and optional treatment columns) to a design data
frame produced by \code{\link[=initialise_design_df]{initialise_design_df()}}. Each split level subdivides the
parent unit (block, or the whole grid if there are no blocks) into smaller
rectangular units of size \code{nrows} by \code{ncols}. Subsequent levels subdivide
the units of the previous level, with the same column-major numbering that
\code{\link[=initialise_design_df]{initialise_design_df()}} uses for blocks.
}
\keyword{internal}
