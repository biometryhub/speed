% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.warn_if_buffers}
\alias{.warn_if_buffers}
\title{Warn when a metric is handed a design that still contains buffer plots}
\usage{
.warn_if_buffers(x, fn)
}
\arguments{
\item{x}{A treatment column.}

\item{fn}{Name of the calling function, for the message.}
}
\value{
\code{invisible(NULL)}, called for the side effect.
}
\description{
Buffer plots are a field-layout convenience, not part of the statistical
design: \code{"buffer"} is not a treatment, and \code{\link[=add_buffers]{add_buffers()}} displaces the real
plots' coordinates to make room for them. \code{summary()} strips them (and undoes
the displacement) via \code{.drop_buffer_rows()}, but a direct call to a metric
does not, so the caller would get a score for a layout that is not the
design.
}
\details{
Shared by the metrics that take a design data frame, so the explanation is
written once.
}
\keyword{internal}
