% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_utils.R
\name{swappable_groups}
\alias{swappable_groups}
\title{Groups Where a Swap Can Still Be Proposed}
\usage{
swappable_groups(design, swap, swap_within, swap_all)
}
\arguments{
\item{design}{Data frame containing the current design}

\item{swap}{Column name of the treatment to swap, or named list for
hierarchical designs}

\item{swap_within}{Column name defining groups within which to swap
treatments, or named list for hierarchical designs}

\item{swap_all}{Whether to swap all matching items or a single item at a time
(default: FALSE)}
}
\value{
A list with:
\itemize{
\item \strong{swappable} - groups holding an exchangeable pair.
\item \strong{unequal_replication} - groups where \code{swap_all = TRUE} rules out every
pair, because no two treatments there share a replication count. Kept
separate from the rest of the unswappable groups because, unlike a group
holding a single treatment, it is rarely what was intended.
}
}
\description{
A group can only be rearranged if it holds two exchangeable treatments.
Whether it does is fixed for the whole of a level, because a level's swaps
permute treatments within a group and so change neither the number of
distinct treatments nor their replication counts. It is therefore settled
once per level rather than rediscovered by sampling.
}
\keyword{internal}
