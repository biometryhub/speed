% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verify_utils.R
\name{.verify_speed_inputs}
\alias{.verify_speed_inputs}
\alias{.verify_hierarchical_inputs}
\alias{.verify_linked_cols}
\alias{.verify_functionally_dependent}
\alias{.verify_optim_params}
\title{Verify Inputs for \code{speed}}
\usage{
.verify_speed_inputs(
  data,
  swap,
  swap_within,
  spatial_factors,
  iterations,
  early_stop_iterations,
  quiet,
  seed
)

.verify_hierarchical_inputs(
  data,
  swap,
  swap_within,
  spatial_factors,
  iterations,
  early_stop_iterations,
  obj_function,
  quiet,
  seed
)

.verify_linked_cols(data, optimise, linked_cols = NULL)

.verify_functionally_dependent(data, swap, col, level)

.verify_optim_params(
  swap_count,
  swap_all_blocks,
  adaptive_swaps,
  start_temp,
  cooling_rate,
  random_initialisation,
  adj_weight,
  bal_weight
)
}
\arguments{
\item{data}{A data frame containing the experimental design with spatial
coordinates}

\item{swap}{A column name of the items to be swapped (e.g., \code{treatment},
\code{variety}, \code{genotype}, etc). For hierarchical designs, provide a named
list where each name corresponds to a hierarchy level (e.g.,
\code{list(wp = "wholeplot_treatment", sp = "subplot_treatment")}).
See details for more information.}

\item{swap_within}{A string specifying the variable that defines a boundary
within which to swap treatments. Specify \code{"1"} or \code{"none"} for no boundary
(default: \code{"1"}). Other examples might be \code{"block"} or \code{"replicate"} or
even \code{"site"}. For hierarchical designs, provide a named list with names
matching \code{swap} to optimise a hierarchical design such as a split-plot.
See details for more information.}

\item{spatial_factors}{A one-sided formula specifying spatial factors to
consider for balance (default: \code{~row + col}).}

\item{iterations}{Maximum number of iterations for the simulated annealing
algorithm (default: 10000). For hierarchical designs, can be a named list
with names matching \code{swap}.}

\item{early_stop_iterations}{Number of iterations without improvement before
early stopping (default: 2000). For hierarchical designs, can be a named
list with names matching \code{swap}.}

\item{quiet}{Logical; if TRUE, suppresses progress messages (default: FALSE)}

\item{seed}{A numeric value for random seed. If provided, it ensures
reproducibility of results (default: \code{NULL}).}

\item{obj_function}{Objective function used to calculate score (lower is
better) (default: \code{\link[=objective_function]{objective_function()}}). For hierarchical designs, can
be a named list with names matching \code{swap}.}

\item{optimise}{Per-level \code{optimise} list as built by \code{\link[=create_speed_input]{create_speed_input()}}.}

\item{linked_cols}{Character vector of column names that should travel with
the \code{swap} column, for example a \code{variety_name} label belonging to a
numeric \code{variety} code. These columns are rearranged alongside \code{swap} and
returned in the optimised order, keeping their original type and position.
They take no part in scoring. For hierarchical designs, provide a named
list with names matching \code{swap} to link different columns at different
levels, e.g.
\code{list(wp = "wholeplot_label", sp = "subplot_label")}; a bare character
vector applies to every level. Columns used as \code{swap}, \code{swap_within} or
spatial factors cannot be linked. On a level with \code{swap_all = TRUE} whole
treatment groups move at once, so a linked column must have exactly one
value per treatment at that level (default: \code{NULL}).}

\item{col}{Name of the linked column being checked.}

\item{level}{Name of the hierarchy level the column is declared on.}
}
\description{
Verify inputs for the \code{speed} function.

Checks the columns named in \code{linked_cols} after they have been merged into
the per-level \code{optimise} list, so all three input shapes are covered by one
set of rules.
}
\keyword{internal}
