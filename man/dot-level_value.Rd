% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.level_value}
\alias{.level_value}
\title{Value a Single Level Takes From an Argument of \code{speed()}}
\usage{
.level_value(value, level, levels)
}
\arguments{
\item{value}{An argument of \code{\link[=speed]{speed()}}, as the user passed it.}

\item{level}{Name of the level being built.}

\item{levels}{Names of every level.}
}
\value{
The value for \code{level}; \code{NULL} where a per-level list omits it, which
leaves the caller to fall back to \code{.DEFAULT}.
}
\description{
A named list naming only levels of the design supplies a value per level;
anything else applies whole to every level. Matching on the level names is
what tells \code{iterations = list(wp = 5, sp = 7)} apart from \code{grid_factors} and
\code{optim_params()}, which are named lists of their own fields rather than of
levels.
}
\keyword{internal}
