% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_utils.R
\name{grid_index}
\alias{grid_index}
\title{Validate a Design's Coordinates and Build its Grid Index}
\usage{
grid_index(df, row_column = "row", col_column = "col")
}
\arguments{
\item{df}{A data frame with columns named by \code{row_column} and \code{col_column}.}

\item{row_column}{Column name of the row position variable (default \code{"row"}).}

\item{col_column}{Column name of the column position variable
(default \code{"col"}).}
}
\value{
A list with \code{idx} (an \code{nrow(df)} x 2 integer matrix of grid
positions), \code{nrow} and \code{ncol} (the grid's dimensions), and \code{n} (the number
of plots the index was built for, used to detect a stale index).
}
\description{
Coerces and validates the \code{row_column}/\code{col_column} coordinates, returning
everything \code{\link[=build_design_matrix]{build_design_matrix()}} needs to place plots on a grid: the
two-column matrix index and the grid's dimensions.

Split out from \code{\link[=build_design_matrix]{build_design_matrix()}} because it is the expensive half and
the \emph{invariant} half. During annealing only the treatment column changes -
the coordinates never do - so the index can be built once per \code{speed()} run
and reused for every iteration. Measured on a 700-plot design, validation and
coercion are ~87\% of the cost of a grid build.
}
\keyword{internal}
