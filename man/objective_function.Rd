% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R, R/speed_df.R
\name{objective_function}
\alias{objective_function}
\title{Default Objective Function}
\usage{
objective_function(adjacency_weight = 1, balance_weight = 1)

objective_function(adjacency_weight = 1, balance_weight = 1)
}
\arguments{
\item{adjacency_weight}{Weight for adjacency score (default: 1)}

\item{balance_weight}{Weight for balance score (default: 1)}

\item{adj_weight}{Weight for adjacency score (default: 1)}

\item{bal_weight}{Weight for balance score (default: 1)}

\item{design}{Data frame containing the current design}

\item{adjacency_list}{List of adjacent plots}

\item{swap}{Column name of the treatment}

\item{spatial_cols}{Character vector of spatial factor column names}
}
\value{
A function which returns numeric value representing the score of the design (lower is better)

Numeric score (lower is better)
}
\description{
A default objective function that combines adjacency and balance scores.
}
\examples{
design_matrix <- matrix(c(1, 2, 2, 1, 3, 3, 1, 3, 3), nrow = 3, ncol = 3)
layout_df <- data.frame(
  row = rep(1:3, each = 3),
  col = rep(1:3, times = 3)
)
objective_function()(design_matrix, layout_df, "treatment", c("row", "col"))

}
