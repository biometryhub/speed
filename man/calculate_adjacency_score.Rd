% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_adjacency_score.R
\name{calculate_adjacency_score}
\alias{calculate_adjacency_score}
\title{Calculate Adjacency Score for Design}
\usage{
calculate_adjacency_score(
  layout_df,
  swap,
  row_column = "row",
  col_column = "col",
  ring_dists = 1,
  ring_weights = 1,
  ring_type = c("manhattan", "chebyshev")
)
}
\arguments{
\item{layout_df}{A data frame containing the design.}

\item{swap}{Column name of the treatments to be scored.}

\item{row_column}{Name of the column representing rows (default \code{"row"}).}

\item{col_column}{Name of the column representing columns
(default \code{"col"}).}

\item{ring_dists}{A vector of positive integers, ring radii to score over.
(default \code{1}, i.e. only the immediate neighbourhood).}

\item{ring_weights}{Per-ring weights aligned with \code{ring_dists} (default
\code{1}).}

\item{ring_type}{Ring shape: \code{"manhattan"} (default; diamond ring)
or \code{"chebyshev"} (square ring). See \code{\link[=ring_offsets]{ring_offsets()}}.}
}
\value{
A non-negative numeric value: the number of like-treatment edges
in the row/column adjacency graph.
}
\description{
Counts adjacent plots — immediate horizontal and vertical neighbours —
that share the same treatment. Lower scores indicate better separation.

Internally this is a thin wrapper around \code{\link[=adjacency_score_vec]{adjacency_score_vec()}}. With
the defaults (\code{ring_dists = 1}, \code{ring_weights = 1},
\code{ring_type = "manhattan"}) it scores the immediate row/column
neighbourhood the simulated-annealing loop in \code{\link[=speed]{speed()}} minimises.
Pass longer \code{ring_dists} / \code{ring_weights} to also penalise like-treatment
matches at larger ring radii. The per-cell score is summed and halved
because each adjacency is counted once from each endpoint.
}
\examples{
# Example 1: design with no like-treatment adjacencies
design_no_adj <- data.frame(
  row = c(1, 1, 1, 2, 2, 2, 3, 3, 3),
  col = c(1, 2, 3, 1, 2, 3, 1, 2, 3),
  treatment = c("A", "B", "A", "B", "A", "B", "A", "B", "A")
)
calculate_adjacency_score(design_no_adj, "treatment") # 0

# Example 2: design with adjacencies
design_with_adj <- data.frame(
  row = c(1, 1, 1, 2, 2, 2, 3, 3, 3),
  col = c(1, 2, 3, 1, 2, 3, 1, 2, 3),
  treatment = c("A", "A", "A", "B", "B", "B", "A", "A", "A")
)
calculate_adjacency_score(design_with_adj, "treatment") # 6

}
\seealso{
\code{\link[=adjacency_score_vec]{adjacency_score_vec()}}
}
