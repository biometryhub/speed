% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{.balance_score_min}
\alias{.balance_score_min}
\title{Smallest Achievable Balance Score}
\usage{
.balance_score_min(layout_df, swap, spatial_cols)
}
\arguments{
\item{layout_df}{A data frame representing the current design}

\item{swap}{A column name of the items to be swapped}

\item{spatial_cols}{Column name(s) of the spatial factors}
}
\value{
A single non-negative numeric value.
}
\description{
Lower bound on \code{\link[=calculate_balance_score]{calculate_balance_score()}} for any arrangement of \code{swap}.
Each level of a spatial factor holds a fixed number of plots, and the
variance of the treatment counts within it is smallest when those plots are
split as evenly as possible across the \code{t} treatments. For a level of \code{n}
plots, the remainder, \code{rem} is \code{n \%\% t}, that minimum has the closed
form \code{rem * (t - rem) / (t * (t - 1))}.

Levels are bounded independently and treatment replication totals are
ignored, so the bound is loose (never wrong) when replication is unequal.
}
\seealso{
\code{\link[=calculate_balance_score]{calculate_balance_score()}}
}
\keyword{internal}
